# 生产环境变量模板
# deploy.sh 会自动生成 .env.prod，通常无需手动编辑
#
# 手动生成强密码命令：openssl rand -base64 32

DOMAIN=yourdomain.com
EMAIL=ops@yourdomain.com

DB_NAME=lemomo_wp
DB_USER=lemomo_prod
DB_PASSWORD=替换为32位随机密码
DB_ROOT_PASSWORD=替换为32位随机密码
