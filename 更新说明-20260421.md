# Lemomo WordPress 更新说明

**更新日期**：2026-04-21  
**交付文件**：`lemomo-update-20260421.zip`

---

## 本次更新内容

### 新增功能

- **表单提交**：新增联系表单功能，访客提交的姓名、邮箱、电话将自动保存至后台「表单提交」菜单，方便统一管理

### 页面更新

- About Us 页面
- Event 页面
- FAQ 页面
- Blog 列表 / 详情页
- Explore 页面
- Header / Footer

### 其他

- 新增多张页面配图（.webp 格式，已优化压缩）
- 样式与交互细节优化

---

## 压缩包内容

```
lemomo-update-20260421.zip
├── wp-content/
│   └── themes/lemomo/       # 完整主题（含全部更新）
└── lemomo_backup.sql         # 数据库备份（可选导入，见下方说明）
```

---

## 更新步骤

> ⚠️ 操作前请先备份服务器上的旧主题和数据库，以便回滚。

### 第一步：上传压缩包到服务器

```bash
scp lemomo-update-20260421.zip root@服务器IP:/opt/lemomo-wordpress/
```

### 第二步：备份旧主题

```bash
cp -r /opt/lemomo-wordpress/wp-content/themes/lemomo \
      /opt/lemomo-wordpress/wp-content/themes/lemomo.bak-20260421
```

### 第三步：解压覆盖

```bash
cd /opt/lemomo-wordpress
unzip -o lemomo-update-20260421.zip
```

### 第四步：修复文件权限

```bash
docker exec lemomo-wordpress \
  chown -R www-data:www-data /var/www/html/wp-content/themes/lemomo
```

### 第五步：验证

浏览器打开网站，检查各页面是否正常显示。  
登录后台 `https://你的域名/wp-admin`，确认左侧菜单出现「表单提交」。

---

## 关于数据库备份（lemomo_backup.sql）

压缩包内附带了最新数据库备份，**通常不需要导入**。

| 情况 | 是否需要导入 SQL |
|------|----------------|
| 只更新主题样式 / 功能 | ❌ 不需要 |
| 服务器数据丢失需要完整恢复 | ✅ 需要 |
| 全新服务器重新部署 | ✅ 需要 |

> ⚠️ 如需导入，请注意：导入 SQL 会**覆盖**数据库中现有的所有内容（文章、页面、表单记录等），请务必提前备份。

如需导入，执行：

```bash
# 进入 MySQL 容器
docker exec -i lemomo-db mysql -u wordpress -pwordpress wordpress < /opt/lemomo-wordpress/lemomo_backup.sql

# 替换域名（将 SQL 中的开发域名替换为正式域名）
docker exec lemomo-db mysql -u wordpress -pwordpress wordpress -e \
  "UPDATE wp_options SET option_value = REPLACE(option_value, 'http://localhost:8888', 'https://你的域名') WHERE option_name IN ('siteurl','home');"
```

---

## 后台使用：表单提交管理

更新完成后，后台新增「表单提交」菜单，用于查看访客通过联系表单发送的询盘。

**查看路径**：后台 → 表单提交 → 所有提交

每条记录包含：
- **Nama**（姓名）
- **Email**（邮箱）
- **Telepon**（电话）
- **提交时间**

> 表单数据只能查看，不支持在后台手动新增。

---

## 技术支持

如更新过程中遇到问题，请联系开发方并提供报错截图。
