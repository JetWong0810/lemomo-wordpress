# Lemomo WordPress 项目交付说明

## 压缩包内容

```
lemomo-wordpress/
├── deploy.sh                    # 一键部署脚本（服务器上运行）
├── docker-compose.prod.yml      # 生产环境 Docker 配置
├── lemomo_backup.sql            # 数据库导出文件（含全部页面和文章数据）
├── nginx/                       # Nginx 反向代理配置
├── certbot/                     # SSL 证书目录
├── wp-content/
│   ├── themes/lemomo/           # 自定义主题（已编译）
│   └── uploads/                 # 媒体文件（图片等）
└── docs/
    ├── deployment.md            # 部署指南（运维必读）
    └── admin-guide.md           # 后台操作指南（内容管理人员必读）
```

---

## 快速部署（3 步）

> 详细说明见 [docs/deployment.md](docs/deployment.md)

### 前提条件

- 一台 Linux 服务器（Ubuntu 22.04 / Debian 12）
- 已安装 Docker 24.0+（含 Docker Compose V2）
- 域名已通过 A 记录解析到服务器 IP
- 防火墙放行 80 和 443 端口

### 步骤

**1. 上传文件到服务器**

```bash
scp -r lemomo-wordpress/ root@服务器IP:/opt/
```

**2. 运行部署脚本**

```bash
cd /opt/lemomo-wordpress
bash deploy.sh
```

按提示输入域名和邮箱，脚本自动完成：SSL 证书申请、数据库导入、URL 替换、权限配置。

**3. 验证**

浏览器打开 `https://你的域名`，确认网站正常显示。

---

## 后台管理

- **后台地址**：`https://你的域名/wp-admin`
- **账号密码**：与开发环境相同（已随数据库一起导入）

后台操作详见 [docs/admin-guide.md](docs/admin-guide.md)，包括：
- 发布 / 编辑博客文章
- 管理媒体报道（外链卡片）
- 修改首页内容（Hero、功能介绍、用户评价等）
- API 接口配置与缓存管理

---

## 技术支持

如部署过程中遇到问题，请参考 [docs/deployment.md](docs/deployment.md) 中的「常见问题」章节。
